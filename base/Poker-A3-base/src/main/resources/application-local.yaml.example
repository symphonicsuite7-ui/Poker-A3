# 本地覆盖示例：复制为 application-local.yaml（已在 .gitignore）
# 启动时加 --spring.profiles.active=local
# 或设置环境变量 A3_DB_PASSWORD / A3_JWT_SECRET

spring:
  datasource:
    password: your-db-password-here

a3:
  jwt:
    secret: please-change-to-a-long-random-secret
